[project]
name = "hatchet-python-quickstart"
version = "0.1.0"
description = "Scheduled Hatchet workflow example"
authors = [{name = "gabriel ruttner", email = "gabe@hatchet.run"}]
readme = "README.md"
requires-python = ">=3.10,<4.0"
dependencies = [
    "hatchet-sdk>=1.33.7",
]

[project.scripts]
manual-run = "src.run:main"
worker = "src.worker:main"
