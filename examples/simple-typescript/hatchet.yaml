dev:
  preCmds: ["pnpm install"]
  runCmd: "pnpm start"
  files:
    - "**/*.ts"
    - "!**/node_modules/**"
  reload: true

triggers:
  - name: simple
    description: "Trigger the first workflow with sample input"
    command: "pnpm run run:simple"
